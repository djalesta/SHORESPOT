<%= simple_form_for [@listing, @booking] do |f| %>
  <%= f.input :date, as: :string, input_html: { value: Date.today, readonly: true } %>

  <%= f.input :start_hour, collection: (10..20).to_a, prompt: 'Select starting hour' %>
  <%= f.input :duration, collection: (1..5).to_a, prompt: 'Select duration' %>

  <%= f.button :submit, "Book spot", class: "primarybuttonblue" %>
<% end %>
